<!-- DeleteBook.ejs -->
<!DOCTYPE html>
<html lang="en">
  <!-- Pass a JavaScript variable to a partial in EJS -->
  <%- include('partials/head.ejs', { title: "Delete Book" }) %>
<body>
  <%- include('partials/navbar') %>

  <div class="content">
    <div class="delete">
      <h2>Delete Book</h2>
      <form action="/single-book/<%= book._id %>?_method=PUT" method="POST">
        <label>Title:</label>
        <input type="text" value="<%= book.title %>" id="title" name="title" required />

        <label>Author:</label>
        <input type="text" value="<%= book.author %>" id="author" name="author" required />

        <label>Genre:</label>
        <input type="text" value="<%= book.genre %>" id="genre" name="genre" required />

        <!-- You can include additional fields for book-specific data -->

        <button type="submit">Delete Book</button>
      </form>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>